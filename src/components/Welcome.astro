---
import Notebox from "./Notebox.astro";
---

<header id="hero">
  <h1><a href="https://tapestries.media">Tapestries</a></h1>
  <h2>An Internet Archive Project</h2>
  <h3>Tagline goes here?</h3>
  <div id="onmobile">
    <Notebox
      >Note that right now tapestries don't work very well on mobile! Look at
      this site on a computer for the full effect.</Notebox
    >
  </div>
</header>

<style>
  section {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  #hero {
    display: flex;
    align-items: start;
    flex-direction: column;
    align-items: center;
    padding: var(--inner-margin);
    border-bottom: 4px dotted var(--accent-color);
    --header-size: 10vw;
    @media screen and (max-width: 1023px) {
      --header-size: 72px;
    }
  }

  h1 {
    font-size: var(--header-size);
    line-height: 1;
    margin: 0;
    font-weight: normal;
  }

  h1 a {
    text-decoration: none;
    color: var(--text-color);
  }

  h2 {
    font-size: calc(var(--header-size) * 0.4);
    font-style: italic;
    font-weight: normal;
    color: var(--accent-color);
    text-align: center;
  }

  h3 {
    text-align: center;
  }

  #onmobile {
    display: none;
    @media screen and (max-width: 1023px) {
      display: block;
    }
  }
</style>
